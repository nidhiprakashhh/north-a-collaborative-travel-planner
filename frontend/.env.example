# Copy to .env.local for local dev (Vite loads .env.local automatically, gitignored by default).
# Same origin as the api's PORT/CORS_ORIGIN setup — Express, Socket.io, and
# the REST API all live on this one server (see api/src/server.ts).
VITE_API_URL=http://localhost:4000
