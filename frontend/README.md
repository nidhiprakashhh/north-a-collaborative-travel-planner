# frontend

React + TypeScript + Vite + Tailwind CSS. Talks to the `/api` service over REST (via React Query) and Socket.io (via `useSocket`/`useTrip`).

```bash
npm install
npm run dev   # http://localhost:5173
```

Requires `VITE_API_URL` — copy `.env.example` to `.env.local`.
